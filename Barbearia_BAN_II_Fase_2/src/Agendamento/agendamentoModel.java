
package Agendamento;

import Conexao.Conexao;
import java.util.HashSet;

/**
 *
 * @author Lenovo
 */
class agendamentoModel {

    static void create(agendamentoBean ab, Conexao con) {
    // encontrar a função equivalente ao prepareStatement SQL
    }

    static HashSet listAll(Conexao con) {
    // encontrar a função equivalente ao createStatement SQL
    return null;  // na verdade, teria uma lista como retorno neste ponto
    }

    static void update(agendamentoBean ab, Conexao con) {
    // encontrar a função equivalente ao prepareStatement SQL
    }

    static void delete(agendamentoBean bb, Conexao con) {
    // encontrar a função equivalente ao prepareStatement SQL
    }
    
}
